{
  "name": "pantaneira-financeiro",
  "version": "1.8.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply pantaneira-financeiro-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply pantaneira-financeiro-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
